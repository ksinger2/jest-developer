{
  "name": "gem-clash-verify",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: JWT purchase verification for Gem Clash (Jest platform)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
